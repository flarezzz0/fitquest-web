
## Stock Alerts Check (3:00 PM ICT)

Checked 7 tickers (AAPL, MSFT, GOOGL, AMZN, NVDA, TSLA, META) for breaking news.

**Significant findings — 3 alerts created:**
1. **MSFT** — Pentagon $9.7B 5-year software license consolidation deal (May 27)
2. **NVDA** — Record Q1 FY2027: $81.6B revenue (+85% YoY), $1.87 EPS beat, dividend 25x, $80B buyback (May 20)
3. **TSLA** — Musk floats Tesla-SpaceX merger ahead of SpaceX IPO (June 12, ticker SPCX, ~$1.75T valuation). Wedbush Ives 80-90% odds.

Alerts saved to `stock-watch/alerts/`. Summary posted to Discord.

## FitQuest Major Updates (Evening, ~16:00-21:00 ICT)

Long session — massive FitQuest overhaul. Key changes:

### Supabase Cloud Sync (Implemented)
- Created `services/supabase.ts`, `.env`, `supabase-schema.sql`, `vercel.json`
- Patched `useStore.ts` at 3 points: import, setUser (Math.max), addWorkout (fire & forget sync)
- Also synced: setProfile, addCoins, spendCoins
- Conflict resolution: streak/Math.max, coins/cloud wins
- **Bug fixed:** `checkHealth()` returned `Promise.resolve(null)` on web → `.then()` fired → `setBackend(true)` → 405 errors. Fixed with `Promise.reject()`

### Calorie Auto-Calc (2024 Compendium MET)
- Dynamic MET based on pace (cardio: 11/8.3/6, walk: 4.3/3.5/2.5)
- Extra: `0.75 × weight × distance` for cardio/walk
- Conditional distance field (only show for cardio/walk)
- Added HIIT (💥 MET 8) + Swim (🏊 MET 6)
- Auto-calc stops if user manually edits calories (resumes on activity change)

### BMR/BMI/TDEE Dashboard
- Compact horizontal layout with Calorie Progress Bar
- Color by %: <25% gray, 25-60% green, 60-85% orange, >85% red
- First-time weight/height setup modal

### Calldash-style UI Redesign
- Profile history: 2-column grid (mobile), 3-column (desktop)
- Cards with pastel bg by fraud score (green/orange/red at 10% opacity)
- Status badges: ✅ผ่าน / ⚠️ปานกลาง / ❌สูง
- Activity + duration + calories + coins per card
- Calendar compacted ~30%
- Upload result screen: clean card design with pill badges

### Other Fixes
- `clearAllData` no longer sets user=null (no logout)
- Streak stuck at 11: `Math.max(local, cloud)` in sync
- vercel.json SPA rewrites for client-side routing
- Health check: `Promise.reject` on web (no 405)
- Mobile quest page: horizontal scroll compact cards

### Known Issues / Pending
- Backend not deployed (local only)
- Logout function not implemented
- `supabase-schema.sql` SQL run by user in Supabase Dashboard
